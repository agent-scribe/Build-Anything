# Netlify configuration for the WeBuild Next.js app.
# The official Next runtime handles SSR, route handlers (streaming), and image
# optimization automatically — no manual function wiring needed.
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
